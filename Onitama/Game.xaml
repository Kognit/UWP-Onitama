<Page
    x:Class="Onitama.Game"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Onitama"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid x:Name="gamegrid" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" SizeChanged="gamegrid_SizeChanged">

        <Grid x:Name="wingrid" Canvas.ZIndex="100" Visibility="Collapsed" Background="#66FFFFFF">
            <TextBlock x:Name="wintext" FontSize="40" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"></TextBlock>
            <AppBarButton HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,80,0,0" Height="20" Click="PlayAgain_Click">
                <TextBlock Margin="0,-15,0,0">Play again!</TextBlock>
            </AppBarButton>
        </Grid>
    </Grid>
</Page>
